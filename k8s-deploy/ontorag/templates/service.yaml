apiVersion: v1
kind: Service
metadata:
  name: {{ include "ontorag.fullname" . }}
  labels:
    {{- include "ontorag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.env.PORT }}
      protocol: TCP
      name: http
  selector:
    {{- include "ontorag.selectorLabels" . | nindent 4 }}
